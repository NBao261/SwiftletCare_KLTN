import { Request, Response } from 'express'
import { notImplemented } from '@/utils/notImplemented.util'

// TODO: Implement in Giai đoạn 2 (Module SALES, §5.10) — stretch, không thuộc phạm vi MVP
export async function verify(_req: Request, res: Response): Promise<void>  { notImplemented(res) }
export async function resolve(_req: Request, res: Response): Promise<void> { notImplemented(res) }
