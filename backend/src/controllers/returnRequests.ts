import { Request, Response } from 'express'

// TODO: Implement in Giai đoạn 2 (Module SALES, §5.10) — stretch, not MVP scope
export async function verify(_req: Request, res: Response): Promise<void>  { res.status(501).json({ error: 'Not implemented yet' }) }
export async function resolve(_req: Request, res: Response): Promise<void> { res.status(501).json({ error: 'Not implemented yet' }) }
